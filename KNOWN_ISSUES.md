# 已知问题记录

## 高优先级

- 控制接口没有鉴权，`CORS(app)` 全开，服务监听 `0.0.0.0:15000`，并且通用静态路由会暴露项目根目录文件。
- 云台控制是开环控制，跟踪逻辑没有读取硬件反馈；串口写线程没有异常处理，断线后可能静默失效。
- 前端先调用真实跟踪接口，再计算轨迹；如果轨迹计算失败，后端跟踪线程仍可能已经开始运行。

## 中优先级

- 强制时间模式默认按北京时间解释，但轨迹接口和启动跟踪接口传参格式不一致，非中国时区浏览器可能出现偏差。
- 前端日志/状态元素 ID 不匹配，部分错误信息不会显示在页面上。
- TLE 解析、过境搜索和阈值逻辑比较粗，需要补充校验、过期判断和更稳的搜索策略。
