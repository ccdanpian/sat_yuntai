# 已知问题记录

## 已处理

- 控制接口支持可选 `SAT_YUNTAI_API_TOKEN`，配置后启动/停止/校准等控制接口需要 `X-API-Token`。
- 静态文件访问已改为白名单路由，不再通过通用路由暴露项目根目录文件。
- 前端启动流程已改为先计算轨迹，确认可用后再启动云台跟踪。
- 串口命令队列已增加异常记录、队列限长和状态上报。
- 卫星不可见时归零指令已改为状态变化时发送，避免每秒重复归零。
- 强制时间接口已统一为 UTC ISO，后端兼容旧的北京时间无时区字符串。
- 星历已改为后端下载和缓存，前端保留浏览器直连作为兜底。
- 星座选择列表已改为从后端同步，减少前后端星座配置不一致。
- 跟踪可见仰角和轨迹最大仰角阈值已改为 `.env` 可配置。
- 云台对准频率已改为 `.env` 可配置，默认 0.02 秒一次；方位角和仰角都小于死区的角度变化会跳过下发。
- 前端状态和调试日志元素 ID 已对齐。

## 待优化

- 云台真实闭环仍需依赖硬件反馈协议确认，目前后端只能确认命令已进入串口发送队列。
- 轨迹搜索仍是粗扫候选段再精算，后续可替换为更严格的升落点求解和 TLE epoch 新鲜度校验。
- `.env` 目前仍被仓库跟踪；如果后续写入真实 token，应迁移为仅保留 `.env.example` 并让部署侧单独维护 `.env`。
